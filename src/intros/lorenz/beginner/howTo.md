#### 生成方法

ローレンツ・アトラクタは、次の連立微分方程式で1つの点を時間発展させて描きます。
$\dot{x} = \sigma(y-x)$、$\dot{y} = x(\rho-z)-y$、$\dot{z} = xy-\beta z$ です。

マンデルブロ集合のようにピクセルごとに計算するのではなく、1点の位置 $(x,y,z)$ を少しずつ進め、その通った道を3次元の曲線として表示します。
この展示では、4次のルンゲ＝クッタ法で安定して積分し、ステップごとに軌道が伸びる様子を見せています。
