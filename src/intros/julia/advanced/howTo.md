#### 数値描画

各ピクセルを初期値 $z_0=x+yi$ とし、固定した $c=a+bi$ に対して

$$z_{n+1}=z_n^2+c$$

を有限回だけ反復します。典型的には発散判定半径を2とし、$|z_n|$ がしきい値を超えた時点の反復回数を色に変換します。

マンデルブロ集合では $z_0=0$ を固定してピクセルごとに $c$ を変えますが、ジュリア集合では $c$ を固定してピクセルごとに $z_0$ を変える、という役割の入れ替えが本質です。
