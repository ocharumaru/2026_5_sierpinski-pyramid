#### 描き方

各ピクセルを複素平面上の点 $z_0=x+yi$ に対応させ、固定した複素数 $c$ で $z \leftarrow z^2+c$ を繰り返します。
値の大きさが発散判定半径を超えたら「外側」と判定し、最後まで超えなかった点を集合の近似として描きます。

マンデルブロ集合ではピクセル座標を $c$ として $z_0=0$ から始めますが、ジュリア集合ではピクセル座標を $z_0$ とし、$c$ は全体で固定します。
