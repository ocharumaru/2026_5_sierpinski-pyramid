この可視化では、一般化 Newton 法

$$
z_{n+1}=z_n-a\cdot\frac{p(z_n)}{p'(z_n)}
$$

を使います。$a=1$ なら通常の Newton 法で、$a$ の実部を大きくすると過剰緩和、小さくすると不足緩和になります。$a$ に虚部を入れると更新方向が複素平面上で回転し、収束領域に渦状の変形が現れます。

プリセットの $p(z)=z^3-2z+2$ は Cayley の例として知られ、根への単純な収束だけでなく 2-サイクルに捕まる初期値が現れます。これは Newton 法が常に根へ向かうわけではない、という重要な注意点を示します。
